# Codex Working Agreement

このリポジトリに対する AI エージェント向けの作業ルールは、この `AGENTS.md` を正本とします。ローカル LLM 運用メモ、PowerShell スクリプト、モデル保存場所、作業用ディレクトリを扱うため、公開してよい設定とローカル専用の状態を分けて作業してください。

## 基本方針

- 応答、進捗報告、完了報告は日本語で行います。
- 作業開始時に `git status --short --branch` を確認し、既存差分を勝手に巻き戻しません。
- 変更は小さく分け、既存の README、`docs/`、`notes/`、`scripts/` の書き方に合わせます。
- 公開可否を確認できない設定、生成物、データ、作業状態は commit しません。
- 公開向けの README、docs、notebooks では、非公開データの具体的な種類を列挙しません。必要な注意喚起は「公開可否を確認済みの教材用サンプルのみ扱う」と一般化します。
- 文章品質、説明の流れ、公開表現、プライバシー境界、用途の一般化を確認する時は、検索結果だけで完了扱いにしません。README、docs、notes、Notebook の Markdown セル、公開される教材データなど、読者が読む箇所を実際に読んで確認します。
- ローカル運用領域は tracked files に大きな生成物や作業ツリーを混ぜないように扱います。
- ルール変更、作業フロー変更、エージェント定義変更を依頼された場合は、先に `AGENTS.md`、`.codex/agents/*`、関連ドキュメントを更新し、その新しいルールで今回の差分を確認します。
- 初心者向け教材では、ユーザーに指摘された制約や注意点を本文に明記するだけで完了扱いにしません。その制約を前提に、読者が実際に体験できる導線、操作、確認方法へ作り替えます。
- Notebook、教材、ツール導線、GUI、外部プロセス起動を含む変更では、構文確認やリンク確認だけで完了扱いにしません。対象セルや手順を実際に実行し、読者が意図した体験を得られるかを evidence として残します。
- 完了報告後にユーザーから手順、セル実行、体験導線、レビュー観点の問題を指摘された場合は、前回レビューの acceptance criteria が不足していたものとして扱います。該当条件を追加し、マルチエージェントフローを再実行してから修正・再検証します。

## 検索・読解・実行確認の使い分け

- `rg` などの検索は、該当候補の発見、既知の語の残存確認、変更後の機械的な再確認に使います。
- 読解は、文章品質、説明の順序、読みやすさ、過不足、トーン、公開表現、用途の一般化、職務・所属・専門領域を推測させる表現、非公開データの種類を示す表現を確認する時に使います。
- 実行確認は、Notebook、手順、スクリプト、GUI、clipboard、外部プロセス起動など、読者の体験や実行結果が受け入れ条件に含まれる時に使います。
- 検索語に依存すると想定外の言い換えや文章全体の不自然さを見落とすため、文章を良くする依頼や公開前確認では、検索だけで判断しません。
- 「残っていないこと」や「全体を確認したこと」を報告する場合は、検索した語だけでなく、実際に読んだ範囲、実行した範囲、未確認の範囲を明記します。
- README、`docs/`、`notes/`、Notebook の Markdown セル、読者が実行・読むプロンプト文字列、公開される `notebooks/data/` は、文章品質や公開表現の修正時に優先して読解します。
- Notebook は JSON 検索だけでなく、Markdown セルと読者が実行・読むプロンプト文字列を確認します。編集後は JSON として読めることも確認します。
- 検索でヒットしなかったことを「全体チェック済み」と表現しません。検索は補助、読解や実行確認が必要な依頼ではそれらを主たる確認として扱います。

## マルチエージェント運用

タスクが Issue 対応、複数ファイル変更、運用ルール変更、スクリプト修正、モデル評価のように失敗時の影響が大きい場合は、親オーケストレータが以下の役割を分けて進めます。再利用用の役割定義は `.codex/agents/*.toml` を正本とします。

- 親オーケストレータ: ユーザーとの窓口、作業方針、進行管理、最終判断、完了報告を担当します。
- 質問担当: 依頼内容、Issue、関連コメント、既存ルールから不足仕様、変更タイプ、検証観点、受け入れ条件を整理します。
- 実装担当: 合意済み仕様に従って編集し、検証結果と evidence handoff を返します。
- 実行体験レビュー担当: Notebook、教材、GUI、clipboard、外部プロセス起動を含む変更で、読者順に実行して意図した体験が得られるか確認します。
- fresh review 担当: 実装者と別視点で、現在差分、関連 docs/tests、検証結果、公開可否の境界を確認します。
- intent review 担当: ユーザーの依頼や会話で固まった意図に、実装が沿っているかを確認します。

サブエージェントは人間へ直接質問しません。質問案、懸念、判断材料は親オーケストレータに返し、親オーケストレータが必要に応じてユーザーへ確認します。

## レビューゲート

commit / push / PR 作成、または完了扱いにする前に、変更の規模に応じて次を実施します。

1. 変更タイプと validation profile を決める。
2. 実装または文書更新を行う。
3. 変更に合う検証を実行する。
4. fresh review で、現在差分が要求と検証観点を満たしているか確認する。
5. intent review で、ユーザーの意図からずれていないか確認する。

どちらかのレビューで blocker が出た場合は、修正してから該当レビューを再実行します。上位指示や実行環境の制約によりサブエージェントを使えない場合は、完了報告で未実施理由と残リスクを明記します。

Notebook や教材の体験導線を変更した場合は、レビュー担当を分けたうえで、少なくとも次を確認します。

1. 実行レビュー: 対象 Notebook のセルを読者の順序で実行し、表示、出力、GUI、clipboard、外部プロセス起動、終了方法が本文の説明と一致するか確認する。
2. fresh review: 実行レビューの evidence と現在差分を別視点で確認し、静的検証だけに偏っていないか確認する。
3. intent review: 実行結果として得られる体験が、ユーザーの依頼や教材の受け入れ条件に沿っているか確認する。

実行レビューでは、親オーケストレータや検証スクリプトに都合のよい作業ディレクトリだけで確認しません。Notebook の配置場所、repository root、repository 外の作業ディレクトリなど、読者が起動し得る cwd の違いを含めて確認します。ユーザーから実行エラーが報告された場合は、その実行条件を acceptance criteria に追加します。

## Validation Profiles

変更タイプごとの主な確認観点です。

- `docs-process`: README、`docs/`、`notes/`、`AGENTS.md`、`.codex/agents/*` の整合性。単体で読める説明になっていること。
- `script-powershell`: PowerShell 構文、引数、エラー時の挙動、既存スクリプトとの使い分け、ローカル環境に依存するパスの扱い。
- `model-config`: Ollama / LM Studio / aider / Cline の設定説明、モデル名、VRAM 前提、環境変数の扱い。
- `benchmark-evaluation`: ベンチマーク条件、測定結果の保存先、再現手順、過剰な生成物を tracked files に入れないこと。
- `tool-integration`: Codex、aider、Cline、GitHub、外部ツール連携の権限境界と手順。
- `notebook-experience`: Notebook や教材の全セル実行、表示出力、GUI/clipboard/外部プロセスの体験、結果の読み方、停止・終了方法、実行後の作業ツリー状態。
- `infra-config`: `.gitignore`、`.env.example`、ディレクトリ構成、ローカル作業領域、改行コード。

## 並列タスク対応

複数 Issue や複数タスクを並列に進める場合は、公開してよい運用ルールを `AGENTS.md` と `.codex/agents/*` に残し、詳細なローカル進行メモは tracked files に置きません。基本単位は次の 1 セットです。

- 1 タスクまたは 1 Issue
- 1 作業ブランチ
- 1 worktree
- 1 Codex 作業スレッド

worktree は repository 内の gitignored な `tmp/worktrees/` に作ります。進行管理スレッドは依存関係、レビュー結果、PR 状態、merge 後の片付けを管理し、各作業スレッドは割り当てられたタスクだけを扱います。

## 完了報告

完了報告には次を含めます。

- 何を変更したか
- 何を確認したか
- fresh review / intent review を実施したか
- 実施していない確認がある場合、その理由
- 残っているリスクや、ユーザー確認が必要な点
