# LM Studio 設定メモ

モデル保存先は次に揃えます。

```text
C:\LLM\lmstudio
```

この PC は RTX 4060 Ti 16GB なので、まずは 13GB から 15GB 程度の量子化モデルを中心に使います。19GB 級は CPU/RAM オフロードが増えやすいため、重要なレビューや品質確認用の候補として扱います。

画像入力や PDF 確認をしたい場合は、LM Studio 側でモデルがマルチモーダル対応として読み込まれているか確認します。
